<project name="Eyekabob" default="all" basedir=".">
    <description>
        Copy the Eyekabob src files in the respective platform directories
    </description>
    <!-- set global properties for this build -->
    <property name="src" location="src"/>
    <property name="lib" location="lib"/>

    <!-- Extraneous target, calls copy-files. -->
    <target name="all" depends="copy-files">
    </target>

    <target name="init" depends="ios-init">
    </target>

    <target name="copy-files" depends="ios-copy-files">
    </target>

    <target name="android-clean">
        <ant antfile="Android/build.xml" target="clean" inheritall="false"/>
    </target>

    <target name="android" depends="android-clean">
        <ant antfile="Android/build.xml" target="debug" inheritall="false"/>
    </target>

    <target name="clean" depends="android-clean, ios-clean">
    </target>

    <target name="ios-init" depends="ios-clean">
        <mkdir dir="iOS/Eyekabob/www"/>
    </target>

    <target name="ios-copy-files" depends="ios-init">
        <copy todir="iOS/Eyekabob/www">
            <fileset dir="${src}">
            </fileset>
        </copy>
    </target>

    <target name="ios-clean">
        <delete dir="iOS/Eyekabob/www"/>
    </target>

    <target name="clean-web">
        <delete dir="web/bin"/>
        <delete file="web/eyekabob.war"/>
    </target>

    <target name="build-web" depends="clean-web">
        <mkdir dir="web/bin"/>
        <javac srcdir="web/src" destdir="web/bin" classpath="web/3rd/json-lib.jar:web/3rd/servlet-api.jar"/>
        <copy file="web/src/index.jsp" todir="web/bin"/>
    </target>

    <target name="war" depends="build-web">
        <war destfile="web/eyekabob.war" webxml="web/eyekabob.xml">
            <fileset dir="web/bin"/>
        </war>
    </target>
</project>
